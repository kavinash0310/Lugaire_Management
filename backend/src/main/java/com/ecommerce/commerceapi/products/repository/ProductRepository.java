package com.ecommerce.commerceapi.products.repository; import com.ecommerce.commerceapi.products.domain.Product; import org.springframework.data.jpa.repository.*; import java.util.*; public interface ProductRepository extends JpaRepository<Product,Long>{ @EntityGraph(attributePaths={"brand","category","variants","variants.color","variants.size"}) Optional<Product> findWithDetailsById(Long id); }
