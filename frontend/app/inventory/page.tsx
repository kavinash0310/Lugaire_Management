"use client";
import {useState} from "react";import {AppShell} from "@/components/app-shell";import {api} from "@/lib/api";
export default function InventoryPage(){const[variantId,setVariantId]=useState("");const[quantity,setQuantity]=useState("");const[type,setType]=useState("PURCHASE");const[message,setMessage]=useState("");const submit=async(e:React.FormEvent)=>{e.preventDefault();try{await api.post("/inventory/transactions",{variantId:Number(variantId),transactionType:type,quantity:Number(quantity),transactionDate:new Date().toISOString().slice(0,10)});setMessage("Stock transaction recorded.")}catch{setMessage("Unable to record stock. Check the SKU variant and available stock.")}};return <AppShell><section className="mx-auto max-w-4xl p-5 md:p-8"><p className="text-sm text-muted-foreground">Inventory</p><h1 className="mt-1 text-2xl font-semibold">Stock management</h1><form onSubmit={submit} className="mt-6 grid gap-4 rounded-lg border bg-card p-6 md:grid-cols-2"><label>Variant ID<input required value={variantId} onChange={e=>setVariantId(e.target.value)} className="mt-1 block w-full rounded border p-2"/></label><label>Transaction type<select value={type} onChange={e=>setType(e.target.value)} className="mt-1 block w-full rounded border p-2"><option>PURCHASE</option><option>MANUFACTURED</option><option>RETURN</option><option>DAMAGE</option><option>ADJUSTMENT_IN</option><option>ADJUSTMENT_OUT</option></select></label><label>Quantity<input required min="1" type="number" value={quantity} onChange={e=>setQuantity(e.target.value)} className="mt-1 block w-full rounded border p-2"/></label><button className="self-end rounded bg-slate-900 px-4 py-2 text-white">Save movement</button></form>{message&&<p className="mt-4 text-sm">{message}</p>}</section></AppShell>}
